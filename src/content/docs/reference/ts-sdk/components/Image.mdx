---
title: Image
---
import { Aside, Code } from '@astrojs/starlight/components';
import imageExample from './examples/image.tsx?raw';

A component for rendering images:
- From external URLs
- From the server hosting a Live Compositor instance

<Aside>To use this component, you need to first register the image with a matching `imageId` using the [`LiveCompositor.registerImage`]() function.</Aside>

## Example

<Code code={imageExample} title='ImageExample.tsx' lang='tsx' collapse={["1-3", "24-48"]} mark='4-6' />

## Props

### id
ID of a component.

- Type: `string`
- Default value: Value produced by useId hook

---

### imageId
ID of an image. It identifies an image registered using a [LiveCompositor.registerImage]() method.

- Type: `string`
