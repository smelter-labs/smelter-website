---
title: Canvas (Web API)
---
import { Aside, Code } from '@astrojs/starlight/components';
import CollapsibleDetails from '../../../../components/CollapsibleDetails.astro';

An output that renders mixed video to the canvas and plays audio using browser API.

<Code title='canvasOutputExample.tsx' code={`
import Smelter from "@swmansion/smelter-web-wasm";
import { View } from "@swmansion/smelter";

async function run() {
    const canvas = document.createElement("canvas");
    const smelter = new Smelter();
    await smelter.init();
    await smelter.registerOutput('example', <View />
      type: 'canvas',
      video: {
        canvas,
        resolution: { width: 1920, height: 1080 };
      };
      audio: true;
    })
}
void run()
`} lang='tsx' />

## RegisterCanvasOutput
<Code code={`import { RegisterCanvasOutput } from "@swmansion/smelter-web-wasm"`} lang='tsx' />

<CollapsibleDetails summaryTitle='Type definitions' open>
<Code code={`
type RegisterCanvasOutput = {
    video?: CanvasOutputVideo;
    audio?: boolean;
};
`} lang='tsx' />
</CollapsibleDetails>

### Properties

#### video
Parameters of an output video.

- **Type**: [`CanvasOutputVideo`](#canvasoutputvideo)

---

#### audio

If `true` `MediaStream` track returned from a register will include audio track.

- **Type**: boolean 

## CanvasOutputVideo

<CollapsibleDetails summaryTitle='Type definitions' open>
<Code code={`
type RegisterCanvasOutput = {
    canvas: HTMLCanvasElement,
    resolution: { width: number; height: number; }
};
`} lang='tsx' />
</CollapsibleDetails>

### Properties

#### canvas 

Canvas element that video should be rendered on.

- **Type**: `HTMLCanvasElement` 

#### resolution

Resolution of an output video

- **Type**: `{ resolution: { width: number; height: number }}`
