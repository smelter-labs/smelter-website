---
title: FFmpeg H264
sidebar:
  order: 1
---

import CollapsibleDetails from '@components/CollapsibleDetails.astro';
import { Aside } from '@astrojs/starlight/components';

## Reference

<CollapsibleDetails summaryTitle='Type definitions' open>

```tsx
type FfmpegH264EncoderOptions = {
  type: "ffmpeg_h264";
  bitrate?: number
  | {
    averageBitrate: number;
    maxBitrate: number;
  };
  preset?:
    | "ultrafast"
    | "superfast"
    | "veryfast"
    | "faster"
    | "fast"
    | "medium"
    | "slow"
    | "slower"
    | "veryslow"
    | "placebo";
  pixelFormat?: "yuv420p" | "yuv422p" | "yuv444p";
  ffmpegOptions?: Record<string, string>;
};
```
</CollapsibleDetails>

## Properties

### bitrate

Desired bitrate of the output stream, in bits per second. If only a numeric value is specified then maximal allowed bitrate is set
to 1.25 of the set number.

- **Type**: `number | { averageBitrate: number; maxBitrate: number; }`
- **Default value**: Calculated based on framerate and resolution. Eg. for 30 FPS in 1080p
  the `averageBitrate` would be 5000 kb/s and `maxBitrate` 6250 kb/s.

---

### Properties

#### averageBitrate

Average bitrate measured in bits per second. Encoder will try to keep the bitrate around the provided average, but may temporarily increase it to the provided max bitrate.

- **Type**: `number`

--- 

#### maxBitrate


Max bitrate measured in bits per second.

- **Type**: `number`

---

### preset

Video output encoder preset. Visit <a href='https://trac.ffmpeg.org/wiki/Encode/H.264#Preset' target='_blank'>FFmpeg docs</a> to learn more.

- **Type**: `"ultrafast" | "superfast" | "veryfast" | "faster" | "fast" | "medium" | "slow" | "slower" | "veryslow" | "placebo"`
- **Default value**: `fast`
- **Supported values**:
    - `ultrafast`
    - `superfast`
    - `veryfast`
    - `faster`
    - `fast`
    - `medium`
    - `slow`
    - `slower`
    - `veryslow`
    - `placebo`

<Aside>

TODO

</Aside>

---

### pixelFormat

Encoder pixel format.

- **Type**: `"yuv420p" | "yuv422p" | "yuv444p";`
- **Default value**: `yuv420p`

<Aside type="caution">

Some H264 encoders may not support `yuv422p` and `yuv444p` formats.
Before setting this option check if it is supported by the used encoder.

</Aside>

---

### ffmpegOptions

Raw FFmpeg encoder options. Visit <a href='https://ffmpeg.org/ffmpeg-codecs.html' target='_blank'>FFmpeg docs</a> to learn more.

- **Type**: `Record<string, string>`

