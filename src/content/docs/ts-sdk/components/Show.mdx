---
title: Show
---
import { Aside, Code } from '@astrojs/starlight/components';
import mp4Example from './examples/Mp4.tsx?raw';
import CollapsibleDetails from '../../../../components/CollapsibleDetails.astro';

A simple component that allows scheduling when element should be added. It allows specifying
timestamp or delay after mount.

This functionality is primarily useful for [offline processing](/fundamentals/glossary#offline-processing).
For live cases the same functionality can be easily reproduced with `useEffect`+`setTimeout`.

## Reference

<Code code={mp4Example} title='Mp4Example.tsx' lang='tsx' collapse={["17-28"]} />

<CollapsibleDetails summaryTitle='Type definitions'>
<Code code={`
type ShowProps = {
    children: ReactNode;
    timeRangeMs?: { start?: number; end?: number };
    delayMs?: number
}
`} lang='tsx' />
</CollapsibleDetails>

## Props

Either `timeRangeMs` or `delayMs` has to be specified.

### children
Content to be displayed if condition specified by `timeRangeMs` or `delayMs` are met.

- **Type**: `ReactNode`

### timeRangeMs
Time range defined by `start` and `end` [timestamps](/fundamentals/glossary#timestamp). At least on of them
needs to be defined.

- **Type**: `{ start?: number; end?: number }`

---

### delayMs
Duration after `Show` component is mounted when children components
will be visible.

- **Type**: `number`
- **Default value**: 1

