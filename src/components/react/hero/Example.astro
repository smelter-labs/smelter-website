---
---

<div class="flex text-start sm:px-14 sm:max-w-[95%]">
    <div class="flex flex-col sm:flex-row gap-8 items-center h-min">
        <div class="gradient-light-1 flex flex-1.5 rounded-4xl p-px h-full w-full">
          <div class="h-full w-full rounded-[calc(2rem-1px)] bg-explanation-background">
            <div class="relative z-0 flex h-full flex-col w-full bg-red-900">
              <div class="flex items-center">
               <video id="example-video" loop autoplay muted class="rounded-4xl h-full w-full">
                      <source src="example-video.mp4" type="video/mp4">
                </video>
              </div>
            </div>
          </div>
        </div>

        <div id="test" class="flex flex-1 items-center">
            <div class="rounded-4xl border border-solid border-example-border overflow-hidden h-full w-full">
                <video id="code-video" loop autoplay muted class="pl-6 pr-4 py-4 h-full w-full">
                    <source src="code-video.mp4" type="video/mp4">
                </video>
            </div>
        </div>

    </div>
</div>

<!-- <script is:inline data-astro-rerun>
  document.addEventListener('astro:page-load', function () {
    const video1 = document.getElementById('example-video');
    const video2 = document.getElementById('code-video');

    video1.addEventListener('ended', () => {
        video1.currentTime = 0;
        video1.play();
        video2.currentTime = 0;
        video2.play();
    });

    video2.addEventListener('ended', () => {
        video1.currentTime = 0;
        video1.play();
        video2.currentTime = 0;
        video2.play();
    });

    video1.play();
    video2.play();
  });
</script> -->